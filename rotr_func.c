#include "monty.h"

/**
 * rotr_func - rotates elements of a stack
 * @stack: pointer to the top of the stack
 * @line_number: line number of code from file read
 */
void rotr_func(stack_t **stack, unsigned int line_number)
{
	stack_t *second, *first = *stack;
	int num;

	(void) line_number;
}
